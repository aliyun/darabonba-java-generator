import Source;
// import Source.ApiUtil; // ApiUtil not needed for this test
import LocalSource;

static function Sample(client: Source): void {
    var runtime = new Source.RuntimeObject{};
    var request = new LocalSource.Request{
        accesskey = "accesskey",
        region = "region",
    };
    // var test = ApiUtil.getQuery(); // ApiUtil not needed for this test
    client.print(runtime);
}